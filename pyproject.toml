[project]
name = "stableswap-tools"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "titanoboa>=0.2.8",
    "vyper>=0.4.3",
]

[dependency-groups]
dev = [
    "ipython>=9.13.0",
    "pytest>=9.0.3",
    "pytest-xdist>=3.8.0",
]
